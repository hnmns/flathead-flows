[project]
name = "flathead-flows"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "altair>=6.2.2",
    "dataretrieval>=1.2.0",
    "polars>=1.42.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
